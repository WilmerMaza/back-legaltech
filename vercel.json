{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm install --include=dev",
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ]
}
